---
- hosts: localhost
  connection: local
#  remote_user: root
#  environment:
#    group_id: "{{ ec2_groups }}"
#    keypair: "{{ ec2_keypair }}"
#    region: "{{ aws_region }}"
#    aws_access_key: "{{ aws_access_key }}"
#    aws_secret_key: "{{ aws_secret_key }}"
  roles:
    - ansible-role-ec2-debian
